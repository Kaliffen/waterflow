---
id:         2026-08-30-7b2c
kind:       fact
atom:       prove
subject:    grafting
lane:       build
tier:       best
revision:   f209827
scope:      [skills/waterflow/references/impressions.md, skills/waterflow/proof-gate.sh, skills/recall/SKILL.md, scripts/test-proof-gate.sh]
supersedes: []
tags:       [prove, grafting, recall]
state:      pass
---
Grafting is implemented across the contract, the gate and `recall`: `npm test`
passed with 16 skills, 11/11 model-invoked, 44 files checked and 19/19 proof-gate
fixtures.

The fixture is the evidence, and it went red first. `scope: [packages/api/b.ts#Thing]`
matched no staged path, so the gate produced no output at all and reported
success — the silent failure the rule exists to prevent, reproduced before it was
fixed. Stripping the symbol before the match turns it green, and the case stays
in `test-proof-gate.sh` beside the `api/` prefix cases that were also real
defects once.

`recall` carries the same strip before `git log`, plus the **broken** marker and
the instruction to read a grafted record's claim from the declaration rather than
from its body.

Anchored at f209827, the commit the work landed in, re-anchored from the commit
before it as the rule under **Staleness** asks.
