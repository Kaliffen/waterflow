---
id:         2026-08-29-d7e1
kind:       fact
atom:       prove
state:      pass
subject:    store-ontology
lane:       build
tier:       best
revision:   531bcd2
scope:      [skills/land/SKILL.md, skills/waterflow/references/impressions.md, AGENTS.md]
supersedes: []
tags:       [prove, store-ontology]
---
The proof for the fold is the two-axis critique, not `npm test`: this slice is
prose, and the eighteen gate fixtures that pass at 531bcd2 are evidence about
the gate, none of which touches consolidation.

Named as a proof kind deliberately. proof.md admits a review with a stated axis
alongside a command, and this is the case it was written for: `land` is a skill,
not a script, so nothing executable can run the fold. Reporting `npm test` here
would have been a pass that cannot fail for the reason the work could be wrong.

The spec axis raised exactly this and was right to. Closing the slice on the
suite would have overstated what was verified, and the store would have carried
a green record whose command never exercised the change.

What the critique did establish: the fold is followable against a real store
after the repair, the trigger no longer fires on untracked subjects, and the two
writes `land` performs are now named in the authoring rules rather than argued
one at a time. What remains unverified is whether the fold produces good
knowledge when run, which is slice store-ontology-5a8d and cannot be answered
here.
