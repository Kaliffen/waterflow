---
id:         2026-08-28-c2a7
atom:       critique
subject:    waterflow-skills
lane:       align
tier:       best
revision:   c6e3515
scope:      [skills/, AGENTS.md, .waterflow/impressions/]
supersedes: []
tags:       [critique, waterflow-skills, reporting, agent-lifecycle]
---
The lifecycle, coordination and reporting change is sound in substance and
duplicated in form: standards found four copies of rules that reporting.md and
topology.md already own, and spec found the atom half of the reporting rule
wired into nothing.

Reviewed at the integration point, `origin/main...main`, two commits and
eighteen files -- not against the last commit. Two axes in parallel subagents,
both joined.

**Standards, four hard violations, all of Authoring Rule 5.** The wait-or-stop
rule exists three times: in topology.md, in `dig`, and in `interrogate`, with
both skills also pointing at topology.md. The pointer being right is the tell
that the copied text should go. The disjoint-scope rule is in both `slice` and
topology.md. Rule 1 and rule 4 of reporting.md are re-listed in all four
composites. The contradiction-is-a-finding rule is in both `critique` and
topology.md. One further violation of Rule 4, composites are thin: `land` and
`build` enumerate what the report must contain, which is a rule about how to
report and belongs wholly in reporting.md.

The judgement calls follow from the same shape: one change touching eight skills
and two references is shotgun surgery, and topology.md now changes for four
reasons and doubled in length.

**Spec, one internal contradiction and two gaps.** reporting.md rule 1 says
"one row per atom that ran" where 9a02 settled "a row per atom including skipped
ones", and the sentence after it in the same file says the skipped atom gets a
row. The file contradicts itself and the decision it implements. Agent
accounting is written as conditional where 9a02 makes it a block element, and
the one worked example in the file omits it, so the example violates rule 4.
9a02 also settled that atoms end with a one-line report; no atom was edited, and
`prove` step 6 still names no marker and links nothing.

**What the field record says.** Three failures in the crowd-sim store this
change was written from are not prevented by it. The wrong-phase proof (e21d,
crowd-0009) is untouched: nothing in the diff edits `prove` or proof.md, and
freshness is still pure revision drift, so a measurement taken in the cheap
opening phase is fresh by that test and still wrong. `revision: unborn` appears
on nine of eleven records and all ten items there, and no rule now requires a
commit before emission -- this repository reproduced that failure inside the
diff itself, which is what c6e3515 exists to close. The ownership collision over
PressureDisplayMax (b6e0) is caught at best partially: the new rule binds agents
to one writer, and the collision was between two peer sessions.

Findings are not fixes. Fixing them is separate work with its own routing.
