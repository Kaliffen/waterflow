---
id:         2026-08-29-a7f1
atom:       prove
state:      pass
subject:    waterflow-skills
lane:       build
tier:       best
revision:   161c75d
scope:      [skills/]
conditions: [target repo waterflowtest2 at 6f8c957, crowd panic sim built through the flow, .NET 10 Release, 32 threads]
supersedes: []
tags:       [prove, waterflow-skills, proof, reporting]
---
Waterflow drove a second app end to end and three of the four claims held: no
record anchored `unborn`, every prove record carried `conditions`, and the two
proofs reproduced exactly. The fourth failed in a way the store could not see.

This is the field proof for 161c75d, run by the owner on a fresh crowd
simulator rather than on this repository. `npm test` cannot produce this
evidence; it checks that the markdown parses.

**Held.** Both prove records carry `conditions` naming scenario, population,
seed, grid, warm-up, measured steps, threads and build. The warm-up reached the
code: the benchmark names it "crowd reaches the barrier and the fire takes
hold" beside a comment saying the number is not a magic one, which is the
2026-08-28-e21d lesson written into the thing that produces the number rather
than into the record about it. No record used `unborn`; supersession chains are
correct; all nine items closed with both `proof` and `revision`.

**Reproduced.** `dotnet test -c Release` gave 38 passed in 11 s against a record
claiming 38 in 11 s. The benchmark gave 3.703 ms mean against a record claiming
3.66, and the recorded conditions were sufficient to reproduce it without asking
anyone anything, which is what the field is for.

**Failed.** Five of nine live records anchored at the commit before the app
existed. That is the finding this revision's successor repairs, settled in
2026-08-29-c4d2.

**Two smaller gaps, both now closed.** The critique ran inline because the
session could not dispatch, disclosed it honestly, and still presented two axes
as though they were independent -- nothing told it that an inline run is not a
two-axis review. And the benchmark printed p99 at 16.07 ms and a worst step at
17.09 ms against a 16.7 ms budget while the record reported the mean and "4.6x
headroom"; a pass that drops frames read as comfortable.

**Checked and not a defect.** The 900-step warm-up is fifteen seconds, the exact
run-in that produced three false passes in the first field run. Here it holds:
measured 3.70 ms at 15 s, 4.01 ms at 40 s and 4.54 ms at 100 s, so the cost
curve is gentle and the pass survives every phase. The record's own `conditions`
made that question askable, and nobody asked it. Recording a condition is the
flow's job; challenging it is the owner's.
