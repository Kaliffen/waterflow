---
id:         2026-08-30-c5a1
kind:       fact
atom:       prove
subject:    waterflow-release
lane:       land
tier:       best
revision:   f209827
scope:      [CHANGELOG.md, package.json, .claude-plugin/plugin.json]
supersedes: [2026-08-29-3c9d]
tags:       [prove, waterflow-release, grafting]
state:      pass
---
Waterflow 0.4.0 is released, and what it contains is the `## 0.4.0` section of
`CHANGELOG.md` — read there, not here.

`npm test` passed: 16 skills, 11/11 model-invoked, 45 files checked, 19/19
proof-gate fixtures. Both manifests carry `0.4.0`.

The contents are held by reference, as the 0.3.0 record held them: the changelog
is the one place a release is described, and a record restating it would be a
second description with nothing keeping the two in step. That is the same
argument grafting generalises to code, which is what this release is.

Anchored at f209827, the release commit itself, re-anchored from the commit
before it — the case the anchor rule was written for, and the same rewrite the
0.3.0 record it supersedes was given. Rewritten in place rather than superseded,
for the reason that record gives: this subject has never had an item on the
state surface, so the fold that would move a spent file to `history` cannot
fire, and a superseding record would leave a file nothing can ever reach. Git
carries the trail instead.
