---
id:         2026-08-28-8a3c
atom:       critique
subject:    waterflow-skills
lane:       build
tier:       best
revision:   5d70160
scope:      [skills/]
supersedes: [2026-08-28-2e6f]
tags:       [critique, waterflow-skills, reporting, proof]
---
Reviewing the whole six-commit diff found four defects no single-commit pass
could see, including the one duplication this work introduced while removing
four others; all four are repaired at 5d70160.

Supersedes 2026-08-28-2e6f, whose open escalation the owner closed at 4f692f0
and whose remaining findings are fixed here. This is the record to read; that
one asserts an escalation that no longer exists.

**What only the integrated view found.** `prove` step 6 grew a copy of
reporting.md's marker table in the same commit that was repairing duplications
elsewhere, and pointed it at impressions.md, which defers markers straight back
to reporting.md. `conditions` disagreed in force across three files -- proof.md
said a measurement carries it, impressions.md and `prove` said optional -- and
the file that owns the concept was the one overstating it. `unborn` reached
impressions.md and `recall` but not proof.md or `land`, so a pass that was
never verifiable read as landable at the one step that checks. Each defect was
introduced in one commit and contradicted by another, which is exactly the shape
a per-commit review cannot see.

**Where the axes disagreed, and who was right.** Standards read `critique`
losing the contradiction rule as a clean repair. Spec read it as an instruction
lost, applying b3e5's own test -- does removing the text lose an instruction or a
restatement -- to a decision made before b3e5 existed. Spec was right: naming a
contradiction is an action the reviewer takes, and it is back, with the rule
still pointed at. The two axes disagreeing on the same hunk and one of them being
correct is the argument for not merging them.

**A finding declined.** Spec argued that `conditions` is still discipline,
because nothing gates a missing one, and that the discipline is what failed three
times in the crowd-sim run. That is a fair reading of the evidence and it
re-opens a decision the owner already made: d5c8 rejected the blocking form
because it puts "is this a measurement?" inside the gate. The answer taken here
is a prompt at step 3, where the number is taken, rather than a gate at step 5 --
which addresses when the phase is captured without changing what is enforced.

**Not fixed, and named.** `recall` restates a rationale that proof.md also
carries, and `build` and `prove` both say a `fail` stops the run. Both are
judgement calls under b3e5's test rather than clear duplications.
