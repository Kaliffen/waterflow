---
id:         2026-08-29-a3b7
kind:       observation
atom:       critique
subject:    store-ontology
lane:       build
tier:       best
revision:   cc9c541
scope:      [skills/waterflow/references/impressions.md, skills/waterflow/proof-gate.sh]
supersedes: []
tags:       [critique, store-ontology]
---
The gate failed open on a typo: an absent `kind` gated and a misspelt one
silently did not, so the quietest possible mistake was also the most dangerous.

Two axes, dispatched in parallel, both joined. Fixed at ca0bb07.

**Corroborated by both axes.** Nothing told an atom which kind to write, so the
contract mandated a field no atom produced and no record in this store carried.
The `watermark` row pointed at a fold section that does not exist -- it belongs
to the consolidation slice. The field table said `always` while the prose thirty
lines below described records that carry none.

**Found by spec alone, and the most valuable finding.** The `case` matched
`"" | fact` and skipped everything else, so `kind: observaton` disabled the gate
for that record without a word. Absent failed closed and malformed failed open,
which is exactly backwards: the two ways a field can be missing should not have
opposite consequences, and the accidental one should never be the permissive
one. The kinds that do not gate are now enumerated and anything unrecognised
gates.

**A weak fixture, kept.** Spec noted the `goal` case exercises the same branch
as the observation case and pairs `atom: prove` with `kind: goal`, which cannot
occur. It proves nothing the observation case does not. It is kept because it
documents intent at the seam cheaply, and the fixture that does carry weight --
the misspelt kind -- was added beside it.

This record is an `observation` rather than a `fact` because a critique is
believed on reading, not on running: nothing in it could have failed. That is
the split the slice exists to make, applied to itself.
