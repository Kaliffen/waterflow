---
id:         2026-08-28-2e6f
atom:       critique
subject:    waterflow-skills
lane:       build
tier:       best
revision:   d5632f0
scope:      [skills/]
supersedes: [2026-08-28-c2a7]
tags:       [critique, waterflow-skills, reporting, parallel-work]
---
Three of the four duplications are genuinely collapsed and the composites are
thin; what remains is one rule still living in two places, one rule deleted
rather than rehomed, and the two axes disagreeing about whether that deletion
was right.

Supersedes 2026-08-28-c2a7. Its reporting, contract and writer findings are
fixed and its `prove` step 6 finding is fixed. The rest carried forward below.

**Fixed.** Reporting rules 1 and 4 are out of all four composites, which are now
bare pointers -- that repair also satisfies Authoring Rule 4, and `land` lost the
rationale sentence it had grown. The wait-or-stop rule is a pointer in `dig`.
The containment rule is a pointer in `critique`. reporting.md no longer
contradicts itself. The contract carries `conditions` and `unborn`, and `recall`
now shows conditions beside the gist rather than in the detail, which was the
half of d5c8 that mattered.

**Still open, and why it is not fixed here.** `slice` keeps the disjoint-scope
rule in operational form -- "either draw the blocking edge or merge them" --
while topology.md states it as a condition. Both axes found this independently,
so it is corroborated rather than two findings. Whether the sentence is `slice`'s
own action or a restatement of topology's rule is a judgement the owner should
make, because removing it leaves `slice` without the instruction that is
arguably its entire job at that point.

**The axes contradict each other**, and that is reported rather than resolved.
Standards read the deletion of `critique`'s contradiction rule as the correct
repair, since topology.md already owns it. Spec read it as unrequested: item
waterflow-skills-c81f asked for one home and a pointer everywhere else, and this
rule alone got neither. Spec is right that the justification -- that critique's
two axes are not independent -- was settled in no record, and it cuts deeper than
the deletion: topology.md legalises `fan-out` only over independent questions, so
if the axes are not independent, `critique` is running a shape the reference does
not sanction. That is escalated, not answered.

**Also carried forward:** `interrogate` restates the composite-never-finishes
rule for an atom, and `land` still duplicates proof.md's "cheap, and always
cheaper than the alternative". Both are judgement calls, neither was in scope.
