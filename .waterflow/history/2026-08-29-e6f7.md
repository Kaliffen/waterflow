---
id:         2026-08-29-e6f7
kind:       fact
atom:       prove
state:      pass
subject:    store-ontology
lane:       build
tier:       best
revision:   e1dcaaa
conditions: [target repo waterflowtest2, crowd-sim subject, 11 records, 9 items all closed, fold run by hand following the shipped prose]
scope:      [skills/waterflow/references/impressions.md, skills/land/SKILL.md]
supersedes: []
tags:       [prove, store-ontology, consolidate]
---
The fold ran end to end on a real store and did what it says: one watermark
moved to history, nothing else was rewritten, and the surviving records are the
subject's live state.

This is the proof 2026-08-29-d7e1 could not give, because it is the one that
executes rather than reviews. Following the shipped prose was enough to do it
without inventing anything -- the trigger was checkable, the classification by
atom worked on a store where every record predates `kind`, and `git mv` kept the
move a rename.

It also produced knowledge that did not exist before. The frame-budget target
was living inside the gist of a prove record -- "4.6x headroom, so no GPU
backend is needed yet" -- and writing it as a goal forced two things into the
open that the sentence had hidden: that the *mean* decides rather than the worst
case, and that at the recorded settings the mean is 3.7 ms while the worst step
is 17.1 ms. The target is met and frames are still dropped. Nobody had to be
wrong for that to go unnoticed; there was simply nowhere to write it.

What it does not establish is that the fold is worth much. See the observation
recorded beside this one.
